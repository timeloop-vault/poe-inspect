// Re-export from the split overlay components for backward compatibility
export {
	ItemOverlay,
	type DisplaySettings,
	defaultDisplay,
	type TradeEditOverlay,
	type ProfileSummary,
} from "./overlay/ItemOverlay";
