# Live Testing Feedback — 2026-03-09

First real in-game testing session with Mirage league items.

## Bugs Fixed This Session

- **StatValue first-match bug**: `find_stat_value` returned on the first matching stat line. Items with duplicate stats (hybrid + pure life) only checked the first. Fixed with `find_matching_stats` iterator.
- **StatValue text matching**: Was using `contains()` on display text. Removed — now matches exclusively by `stat_id`. UI auto-resolves stat_id via `resolve_stat_template`.

## Parse Failures (poe-item grammar gaps)

Items that don't show in the overlay because poe-item can't parse them:

| # | Item Type | Notes |
|---|-----------|-------|
| 1 | Flask enchantments | Enchant section on flasks not handled |
| 2 | Currency (general) | Chaos Orb, Divine, etc. — simpler than coffin fixture |
| 3 | Corrupted tag | `Corrupted` status not rendering in overlay? |
| 4 | Gems | Skill/support gems fail to parse or display |
| 5 | Coin of Restoration | New 3.28 item type — not in grammar |
| 6 | Tattoos | League-specific item type |
| 7 | Runecraft | New item type |
| 8 | Essences | Currency-like with special behavior |
| 9 | Scarab mods | Scarab items with map mods |
| 10 | Divination Cards | Exist in fixtures but may fail in overlay |
| 11 | Oils | Currency-like |
| 12 | Catalysts | Currency-like with quality type |

**Action**: Add fixtures for each, identify grammar gaps, fix poe-item.

## Evaluation Issues

### Hybrid mods not supported
Hybrid mods (e.g., "Increased Physical Damage + to Accuracy Rating" on a staff) are treated as separate one-line mods during parsing, but they share the same prefix/suffix slot. This means:
- Open mod count may be wrong (counting one hybrid as two mods)
- StatValue matching works (finds the stat line) but mod-level predicates like `ModCount` or `ModTier` may double-count
- poe-item's resolver needs to merge stat lines that belong to the same hybrid mod group

**Action**: Investigate how Ctrl+Alt+C text represents hybrid mods. Add fixtures, update poe-item resolver to group them correctly.

## UI Issues

### Stat search dropdown too short
The suggestions dropdown for stat templates (e.g., when picking "Maximum Life" in a StatValue rule) is truncated — entries show as `# to Maximum L...` and many look identical. Needs to be wider or show the full text.

**Screenshot**: `Skärmbild 2026-03-09 220854.png`

### Overlay has gray tint over item card
The fullscreen semi-transparent gray backdrop (click-to-dismiss area) is also applied on top of the item overlay card, muting the PoE-native colors. The in-game tooltip has vivid gold/brown header colors; our overlay looks washed out.

**Fix**: The overlay card (`item-card`) needs to sit above the gray backdrop, or the backdrop should have a cutout/exclude around the card. Check z-index and background layering.

**Screenshots**: Overlay (`Skärmbild 2026-03-09 222703.png`) vs in-game (`Skärmbild 2026-03-09 222710.png`)

### Tier/Rank color scale needs rework
Current tier coloring uses a flat 4-level system (T1 = best, T2-3, T4-5, low). Problems:

1. **Mod-relative scaling**: Each mod has its own tier range. A mod with only T1-T3 means T3 is the worst, not "mid". The color should reflect position within that mod's range, not absolute tier number.
2. **Rank vs Tier**: Crafted mods and some influence mods use Rank instead of Tier, where higher = better (Rank 3 > Rank 1). Currently colored incorrectly.
3. **Data source**: poe-data should own a helper that returns a normalized quality level given a mod's tier/rank and the full tier range for that mod family. App just maps quality → color.

**Action**: Add tier range data to poe-data's `domain.rs` or compute from Mods table. Expose `tier_quality(mod_name, tier) -> Quality` that accounts for tier count and rank direction.

## Future Features

### Pseudo (sum) stats
Like poe.trade's pseudo mods — `pseudo total life > 140` sums all matching stat lines across mods. New predicate type `PseudoStatValue` in poe-eval. The term "pseudo" is established PoE community convention.

### Item browser for rule building
When building rules (e.g., for shields), users need to look up base types, mods, etc. Currently requires googling. We have all this data in poe-data — could render a browsable item/base type database with PoE assets. Useful for Base Type field in rules ("what's the best armour shield base?").

### HasStatText deprecation
`HasStatText` predicate uses text `contains` matching. Should be deprecated in favor of `HasStatId` (already exists). No new rules should use text matching.
